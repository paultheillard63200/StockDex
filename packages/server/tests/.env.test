# Variables chargées par `node --env-file=tests/.env.test` avant que
# n'importe quel module n'soit évalué. Cela permet à db/index.js de pointer
# vers une base éphémère et à lib/auth.js d'utiliser un coût bcrypt faible.
JWT_SECRET=test-secret-do-not-use-in-prod
JWT_EXPIRES_IN=1h
BCRYPT_ROUNDS=4
